# Core Kotlin - DDD Member Management System

Spring Boot + Kotlin으로 구현한 회원 관리 시스템입니다. Domain-Driven Design (DDD) 아키텍처를 따르며, 청계층별로 모듈이 분리되어 있습니다.

## 📁 프로젝트 구조

```
core-kotlin/
├── core-domain/           # 도메인 계층 (순수 Kotlin, 외부 의존 없음)
├── core-application/      # 응용 계층 (비즈니스 로직, 트랜잭션)
├── core-infra/           # 인프라 계층 (DB, Redis, Kafka, MongoDB)
├── core-presentation/    # 표현 계층 (REST API)
└── build.gradle.kts      # 루트 빌드 설정
```

## 🏗️ 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                    의존성 방향 (DDD)                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐      │
│   │ PRESENTATION│────→│ APPLICATION │────→│   DOMAIN    │      │
│   │   (Web)     │     │  (Service)  │     │  (Pure Kt)  │      │
│   └─────────────┘     └─────────────┘     └─────────────┘      │
│          ↓                                        ▲             │
│   ┌─────────────┐                               │              │
│   │   INFRA     │───────────────────────────────┘              │
│   │ (JPA/NoSQL) │                                               │
│   └─────────────┘                                               │
└─────────────────────────────────────────────────────────────────┘
```

## 🚀 시작하기

### 전제 조건

- JDK 21+
- Gradle 8.14+

### 빌드

```bash
./gradlew build
```

### 테스트

```bash
# 전체 테스트
./gradlew test

# 특정 모듈 테스트
./gradlew :core-domain:test
./gradlew :core-application:test
./gradlew :core-infra:test
```

### 실행

```bash
./gradlew :core-presentation:bootRun
```

API가 `http://localhost:8080`에서 실행됩니다.

## 📦 모듈별 의존성

| 모듈 | 의존성 | 역할 |
|------|--------|------|
| **core-domain** | 없음 | 도메인 모델, Value Object |
| **core-application** | domain | 비즈니스 로직, @Service, @Transactional |
| **core-infra** | domain | JPA, Redis, Kafka, MongoDB |
| **core-presentation** | application | REST Controller, DTO |

## 🔧 외부 서비스

| 서비스 | 포트 | 용도 |
|--------|------|------|
| PostgreSQL | 5432 | 주요 DB |
| H2 | - | 테스트용 인메모리 DB |
| Redis | 6379 | 캐싱 |
| Kafka | 10000-10002 | 이벤트 메시징 |
| MongoDB | 27017 | 이벤트 저장소 |

## 📝 API 예시

```bash
# 회원 생성
curl -X POST http://localhost:8080/api/members \
  -H "Content-Type: application/json" \
  -d '{"email": "test@example.com", "name": "홍길동"}'

# 회원 조회
curl http://localhost:8080/api/members/1

# 전체 회원 조회
curl http://localhost:8080/api/members

# 회원 정보 수정
curl -X PATCH http://localhost:8080/api/members/1 \
  -H "Content-Type: application/json" \
  -d '{"name": "김철수"}'

# 회원 상태 변경
curl -X PATCH http://localhost:8080/api/members/1/status \
  -H "Content-Type: application/json" \
  -d '{"status": "INACTIVE"}'

# 회원 삭제
curl -X DELETE http://localhost:8080/api/members/1
```

## 🧪 테스트 전략

- **Domain Layer**: 순수 Kotlin 테스트 (Mockito)
- **Application Layer**: 서비스 로직 테스트
- **Infra Layer**: JPA Repository 테스트 (@DataJpaTest)
- **Presentation Layer**: Controller 테스트 (@WebMvcTest)

## 📄 문서

- [아키텍처 상세](docs/ARCHITECTURE.md)
- [개발 가이드](docs/DEVELOPMENT.md)

## 라이선스

MIT License
